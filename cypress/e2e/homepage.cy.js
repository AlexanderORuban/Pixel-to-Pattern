describe('can visit the page', () => {
  it('page appears', () => {
    cy.visit('http://localhost:3001')
  })
})

// can create post
  // can view post
// can delete post
  // can no longer view post